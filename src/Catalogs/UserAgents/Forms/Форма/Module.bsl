
&НаСервере
Процедура ЗагрузитьНаСервере()
	
	НаборПрокси = СтрРазделить(СписокПрокси, Символы.ПС, Ложь);
	
	Для Каждого Прокси Из НаборПрокси Цикл
		
			НовыйПрокси = Справочники.UserAgents.СоздатьЭлемент();
			НовыйПрокси.Агент 			= Прокси;
			НовыйПрокси.Наименование 	= Прокси;
			НовыйПрокси.Записать();	
			
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Загрузить(Команда)
	ЗагрузитьНаСервере();
КонецПроцедуры



