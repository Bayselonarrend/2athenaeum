Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	МЗ = РегистрыСведений.ИспользованиеПабликовМаскировки.СоздатьМенеджерЗаписи();
	МЗ.ПоследнееИспользование = ТекущаяДатаСеанса();
	МЗ.Паблик = Ссылка;
	МЗ.Записать(Истина);
	
КонецПроцедуры